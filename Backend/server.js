// require("dotenv").config()
// const app = require("./src/app")

// app.listen(3000, () => {
//     console.log("server is running on port 3000");
// })       

require("dotenv").config();

const app = require("./src/app");

module.exports = app;